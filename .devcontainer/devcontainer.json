{
    "name": "lambda-cryptography-layer",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "runArgs": ["--init"],
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [],
    "forwardPorts": [],
    "postCreateCommand": "sudo yum update -y && sudo yum install -y docker && sudo systemctl enable docker && sudo systemctl start docker",
    "remoteUser": "vscode"
}